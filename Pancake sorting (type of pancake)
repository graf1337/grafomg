def flip(arr, k):
    arr[:k] = arr[:k][::-1]

def pancake_sort(arr):
    n = len(arr)
    for size in range(n, 1, -1):
        max_idx = arr.index(max(arr[:size]))
        if max_idx != size - 1:
            flip(arr, max_idx + 1)
            flip(arr, size)
    return arr
